package com.example.familylocationalert.data

object DefaultLocations {

    val all: List<LocationPoint> = listOf(
        LocationPoint(
            id = "teste",
            name = "Teste",
            latitude = 20.000000,
            longitude = -9.000000,
            radiusMeters = 200f,
            phoneNumbers = emptyList()
        )
    )
}